# Tasks: Style Hidden Pieces

## Implementation
- [x] Update `Cell.tsx` to use `Piece` component for hidden pieces (`client/src/components/setup/Cell.tsx`).
- [x] Verify `Piece.tsx` handles `hidden` type correctly (rendering generic icon/color) (`client/src/components/setup/Piece.tsx`).

## Verification
- [x] Verify hidden opponent pieces appear as colored circles (Red/Blue).
- [x] Verify they do NOT show their rank (Rock/Paper/Scissors).
- [x] Verify they show the generic hidden icon (`?`) or valid placeholder.
