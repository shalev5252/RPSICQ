# Tasks: Rules Diagram

## Shared
- [x] Ensure `RPSLS_WINS` is accessible (already done in shared).

## Client - Components
- [x] Create `RulesModal.tsx` and `RulesModal.css`.
- [x] Implement Diagram Logic:
    - [x] `Classic` mode layout (Triangle).
    - [x] `RPSLS` mode layout (Pentagon).
    - [x] (Optional) Add interactivity (hover to see strengths).
- [x] Add `RulesButton` to `SetupScreen.tsx`.
- [x] Add `RulesButton` to `GameScreen.tsx`.

## Styling
- [x] Style the modal to match game theme (Dark/Glassmorphism).
- [x] Ensure responsive design (fitting on mobile).

## Verification
- [x] Verify button opens modal in Setup.
- [x] Verify button opens modal in Game.
- [x] Verify correct rules shown for Classic.
- [x] Verify correct rules shown for RPSLS.
